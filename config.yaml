output_dir: "causal_pfn_data"
hidden_dim: 16
n_nodes_min: 8
n_nodes_max: 16
p_edge: 0.3
p_linear: 1.0 # Curriculum Learning: Start with Linear only
n_samples: 1000
n_runs: 50
min_nodes_large: 50
max_nodes_large: 100

# Training# Model Architecture
max_cols: 128
min_nodes: 8
embed_dim: 256 # Increased from 128 for more capacity
n_heads: 8 # Increased from 4
n_layers: 6 # Increased from 4

# Training
batch_size: 8 # Physical batch size (keep small for memory)
accumulation_steps: 4 # Effective batch size = 8 × 4 = 32
use_amp: false # Mixed Precision (DISABLED due to NaN)
lr: 0.001 # Increased 10x to escape local minimum
total_steps: 20000 # Increased from 2000 for better convergence
print_every: 100
n_test_samples: 100
device: "cuda" # Set to "cuda" if you have enough GPU memory (currently < 100MB free)
